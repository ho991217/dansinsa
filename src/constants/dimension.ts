export const WIDTH = 1080;
export const HEIGHT = 2160;
export const RATIO = WIDTH / HEIGHT;
const CAM_DEMENSION = {
  width: WIDTH,
  height: HEIGHT,
};

export default CAM_DEMENSION;

export const CROP_SIZE = {
  width: WIDTH,
  height: 1440,
}
